// all java code is defined in a class
// a class describes how to make an object of that class type....A class is like
// blueprint
// Instanc variables : things an object knows about itself is called
// instance(object's state)
// variables..they represent the state of an object
// things an object does are called methods....they represent the behaivour of
// an object(what an object can do ??)

// Example : Alarm object have an instance variable to hold the
// alarmTime.....and 2 methods for getting and setting the alarm time
// .opearator gives you access to an object' state and behaivour

// Syntax : ClassName objectName = new ClassName();

// If a method takes a parameter,you must pass it something when you call it.
// parameter is a variable that is used inside the body of the method....with
// type and name
// Arguments :actual values jo pass krte h------jab method call krte ho jab jo
// values dete ho...add(5,10);

// a method must declare a return type...a void type means that the method does
// not return anything

// Instance variables always get a default value....if you don't explicilty
// assign a value to an instance variable or
// youdon't call a setter method...the instance variable still has a
// value(default value)

// default value : integer - 0, floating poins - 0.0, booleans - false ,
// references - null

// Local Variables :-
// Declared inside a method, constructor, or block
// Accessible only within that method/block
// Created when the method starts, destroyed when it ends
// Must be initialized before use
// they are temporary and live only as long as the method is on the stack (in
// other words, the method hs not reached the closing curly braces)
